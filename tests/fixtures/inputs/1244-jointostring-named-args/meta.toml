name = "1244-jointostring-named-args"
status = "supported"
features = ["joinToString", "named-args", "default-params"]
reason = "Locks in the named-arg → slot routing for `joinToString$default`. Without it, named args were dropped onto the first positional slot, producing garbled output."
