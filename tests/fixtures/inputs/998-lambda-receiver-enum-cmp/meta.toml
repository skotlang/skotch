name   = "998-lambda-receiver-enum-cmp"
status = "supported"
features = ["lambda", "enum", "generics"]
notes = "Lambda comparing a passed object's property to an enum constant (`it.s == V.A`). Enum-entry singletons make the reference `==` correct across the call into the lambda. Outputs 1."
