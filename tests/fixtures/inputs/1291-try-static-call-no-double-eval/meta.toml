name = "1291-try-static-call-no-double-eval"
status = "supported"
features = ["call-dispatch", "side-effects", "mir-lower"]
reason = "Locks in mir-lower fix: `try_java_static_call` no longer lowers call args before knowing the dispatch will succeed, avoiding a duplicate `Call` MIR statement when the probe misses and the caller falls through to the regular instance-method path."
