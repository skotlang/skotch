name = "1290-while-true-with-var-pos-update"
status = "supported"
features = ["while-true", "var-field", "use-before-def", "stackmap"]
reason = "Locks in two related fixes for the `while (true) { … return; var = … }` pattern: (1) use-before-def analysis skips unreachable predecessors so the unreachable exit_block's empty entry_defs don't poison reachable join points; (2) backend stackmap live-slot dataflow does the same."
