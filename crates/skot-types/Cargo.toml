[package]
name        = "skot-types"
version     = { workspace = true }
edition     = { workspace = true }
license     = { workspace = true }
description = "Type lattice (no inference engine) for skot"

[dependencies]
skot-intern = { workspace = true }
serde       = { workspace = true }
