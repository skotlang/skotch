[package]
name        = "skotch-config"
version     = { workspace = true }
edition     = { workspace = true }
license     = { workspace = true }
description = "Workspace-wide constants for skotch (class file versions, DEX versions, Android API levels)"
