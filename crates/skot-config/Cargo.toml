[package]
name        = "skot-config"
version     = { workspace = true }
edition     = { workspace = true }
license     = { workspace = true }
description = "Workspace-wide constants for skot (class file versions, DEX versions, Android API levels)"
