[package]
name        = "skotch-types"
version     = { workspace = true }
edition     = { workspace = true }
license     = { workspace = true }
description = "Type lattice (no inference engine) for skotch"

[dependencies]
skotch-intern = { workspace = true }
serde       = { workspace = true }
